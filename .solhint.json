{
  "extends": "solhint:recommended",
  "rules": {
    "compiler-version":   ["error", "^0.8.0"],
    "func-visibility":    ["error", { "ignoreConstructors": true }],
    "no-empty-blocks":    "error",
    "reason-string":      ["warn",  { "maxLength": 64 }],
    "natspec-comments":   "warn",
    "ordering":           "warn"
  }
}
